============
Single Point
============

A minimal example configuration file for ``janus singlepoint`` is:

.. literalinclude:: minimal/singlepoint.yml

An example configuration file with all parameters set for ``janus singlepoint`` is:

.. literalinclude:: full/singlepoint.yml
