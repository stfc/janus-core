===================
Nudged Elastic Band
===================

A minimal example configuration file for ``janus neb`` is:

.. literalinclude:: minimal/neb.yml

An example configuration file with all parameters set for ``janus neb`` is:

.. literalinclude:: full/neb.yml
